apiVersion: v1
kind: Service
metadata:
  name: quiz-db
  namespace: databases
spec:
  selector:
    app: quiz-db
  ports:
  - port: 1433
    targetPort: 1433
    name: mssql
  clusterIP: None
  type: ClusterIP

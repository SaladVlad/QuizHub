apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: databases
spec:
  selector:
    app: user-db
  ports:
  - port: 1433
    targetPort: 1433
    name: mssql
  clusterIP: None
  type: ClusterIP

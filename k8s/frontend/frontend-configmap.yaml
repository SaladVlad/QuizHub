apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: quizhub
data:
  config.js: |
    // Runtime configuration - this file is loaded at runtime, not build time
    window.RUNTIME_CONFIG = {
      API_BASE_URL: 'http://quizhub.local/api',
      IS_PRODUCTION: true
    };
